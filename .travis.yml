dist: xenial
addons:
  snaps:
  - name: node
    confinement: classic
    channel: 12
language: rust
rust:
  - stable
cache:
  cargo: true

before_script:
 - "cargo run"
 - sleep 10

install:
  - git submodule add https://github.com/JDSeiler/alexandria-tests

script:
  - pushd alexandria-tests && npm test
